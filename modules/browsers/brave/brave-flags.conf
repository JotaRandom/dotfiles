# Brave Browser Flags - Optimizado para ThinkPad L420 (1366x768, RAM limitada)
# Aplicar: brave --flag-file=/path/to/brave-flags.conf
# O agregar a .desktop file

# ===== PERFORMANCE & RAM =====
# Memory saver (libera RAM de tabs inactivos)
--enable-features=HighEfficiencyModeAvailable,TabDiscarding
--memory-pressure-thresholds=0.5,0.7,0.9

# Parallel downloading
--enable-parallel-downloading

# Reducir cache (RAM limitada)
--disk-cache-size=52428800  # 50MB vs default ~250MB

# ===== GPU & RENDERING =====
# Intel HD Graphics optimization
--use-gl=desktop
--enable-gpu-rasterization
--enable-zero-copy
--ignore-gpu-blocklist

# Smooth scrolling OFF (performance)
--disable-smooth-scrolling

# ===== UI COMPACTA (1366x768) =====
# Start maximized
--start-maximized

# Dark mode
--force-dark-mode
--enable-features=WebUIDarkMode

# Tab strip compacto
--compact-tab-strip

# ===== BRAVE ESPECÍFICO =====
# Hardware media keys
--enable-features=HardwareMediaKeyHandling

# ===== PRIVACY & PERFORMANCE =====
# Deshabilitar servicios innecesarios
--disable-background-networking
--disable-features=MediaRouter

# DNS over HTTPS
--enable-features=DnsOverHttps

# ===== OTROS =====
# GTK version
--gtk-version=4
