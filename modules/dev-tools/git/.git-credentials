# Ejemplo de ~/.git-credentials
# Este archivo es una plantilla/ejemplo — NO subas secretos aquí.
# Formato (no copies secretos):
# https://<usuario>:<personal-access-token>@github.com
# Marcador de ejemplo (no funcional):
# https://tu-usuario:tu-token@github.com

# Si prefieres el formato clave/valor soportado por git credential-store, usa:
# protocol=https
# host=github.com
# username=tu-usuario
# password=tu-token
