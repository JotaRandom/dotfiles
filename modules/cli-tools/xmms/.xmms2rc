# Ejemplo mínimo de configuración de XMMS2
# Ruta por defecto: ~/.xmms2/config (este archivo lo enlaza el instalador en $HOME/.xmms2rc)
# Este archivo es un ejemplo mínimo para establecer un directorio de música por defecto y el volumen
player.audio.volume = 80
player.audio.output_device = "default"
library.dir = "$HOME/Music"

# Configuración de red: habilitar el modo cliente-servidor (opcional)
daemon.enabled = true
daemon.listen = "/run/user/$UID/xmms2.sock"

# Usa el manejador de URL de libc para archivos locales
io.auto_buffer = true

# Fin del archivo
